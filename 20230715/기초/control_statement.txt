제어문

break
실행 흐름이나 반복 중인 루프를 조건식의 결과에 상관없이 즉각 종료 시킨디.

continue
이 구문 아래에 있는 실행 구문들을 건너뛰고 다음 반복을 시작하는 역할을 한다.

fallthrough
앞에서 switch 구문의 흐름을 제어할 때 다룬 구문으로, 일치된 case 블록의 실행을 다음 case 블록으로 전달할 때 사용한다.

return
함수와 메소드에서 사용되는 구문이다. 함수와 메소드 내에서 값을 반환하면서 실행을 종료하거나 단순히 코드의 실행을 종료하는 역할